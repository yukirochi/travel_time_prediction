version: 2

sources:
  - name: raw_data  # This is the alias/nickname you use in your SQL
    database: training_data    # The actual database name in Snowflake
    schema: raw   # The actual schema name in Snowflake
    
    tables:
      - name: raw_table      # The actual table name in Snowflake
